# 验收清单

- 环境：`java -version`、`javac -version`、`mvn -v`、`mysql --version` 可用。
- 数据库：`q_itoffer` 库存在，`users`、`companies`、`jobs`、`applications` 有演示数据。
- 前台：职位搜索、企业浏览、登录注册、简历编辑、职位申请、我的申请可用。
- 后台：管理员登录、工作台统计、企业管理、职位管理、简历申请处理、用户管理、系统日志可用。
- 构建：`npm run build` 和 `mvn package` 通过。

